# BrowserNotes Chrome Extension

Clip text, images, and selections from any web page directly into BrowserNotes. All data stays local.

## Install

1. **[Download browsernotes-chrome.zip](./browsernotes-chrome.zip)**
2. Unzip the file
3. Open `chrome://extensions` in Chrome
4. Enable **Developer mode** (toggle in top-right)
5. Click **Load unpacked**
6. Select the unzipped folder

## Features

- **Right-click to save** — Select text on any page, right-click, and choose "Save to BrowserNotes"
- **Keyboard shortcut** — `Ctrl+Shift+N` (Mac: `Cmd+Shift+N`) to capture the current selection
- **Rich content** — Preserves formatting, links, and inline images as Markdown
- **Confirmation bubble** — Visual feedback after each capture
- **Send to BrowserNotes** — Transfer all captured clips to the web app with one click

## Usage

After installing, browse any page and select text you want to save. Use the right-click menu or keyboard shortcut to capture it. Open the extension popup to see recent captures and send them to BrowserNotes.
